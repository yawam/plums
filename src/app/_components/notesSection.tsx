interface NotesSectionProps {
  topicId: string;
}

const NotesSection = () => {
  return (
    <div className="w-full bg-fuchsia-900 shadow-xl rounded-xl h-[100px] text-white ">
      NotesSection
    </div>
  );
};

export default NotesSection;
